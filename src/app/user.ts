export class User {
    constructor (
        public name?: string,
        public username?: string, 
        public email?: string,
        public userExperience?: number,
        public isAdmin?: boolean
    ){}
}
